<main class="app-content">
  <div class="dashboard-card">
    <h1 class="mb-3">Bienvenido, <%= user.fullname ? user.fullname.split(' ')[0] : user.email %> 👋</h1>

    <p class="mb-2 text-muted">
      Tu email: <span class="fw-semibold"><%= user.email %></span>
    </p>
    <p class="mb-4 text-muted">
      Último inicio de sesión:
      <span class="fw-semibold"><%= user.lastLogin ? user.lastLogin.toLocaleString() : '—' %></span>
    </p>

    <div class="dashboard-links d-flex flex-wrap align-items-center gap-2">
      <!-- Botón principal: ir al juego -->
      <a href="/game" class="btn-pill-main">
        Ir al juego
      </a>

      <!-- Secundario: perfil -->
      <a href="/profile" class="btn-pill-secondary">
        Ver perfil
      </a>

      <!-- Enlace fantasma: opcional -->
      <a href="/" class="btn-pill-ghost">
        Volver al inicio
      </a>
    </div>
  </div>
</main>
