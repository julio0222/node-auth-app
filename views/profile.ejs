<div class="row justify-content-center">
  <div class="col-lg-8 col-xl-7">
    <div class="card-glass">
      <h1 class="card-title-main mb-3">Perfil de <%= user.fullname || 'usuario' %></h1>

      <dl class="row mb-0">
        <dt class="col-sm-3 text-secondary">Email</dt>
        <dd class="col-sm-9"><%= user.email %></dd>

        <dt class="col-sm-3 text-secondary">Registrado</dt>
        <dd class="col-sm-9"><%= user.createdAt ? user.createdAt.toLocaleString() : '—' %></dd>
      </dl>
    </div>
  </div>
</div>
